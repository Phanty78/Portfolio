import {
  toggleActiveFilter as i,
  createFilters as l,
  getData as r,
  displayElements as s,
} from './shared.js'
let skillsURL = '/data/skills.json',
  skillsContainer = document.querySelector('.skills'),
  skillsFilterContainer = document.getElementById('skills__filters__container'),
  filtersArray = ['All', 'Front', 'Back', 'Tool', 'Soft'],
  filterTag = 'All',
  fileName = 'skills'
r('/data/skills.json')
  .then((r) => {
    if (r) {
      r.forEach((l) => {
        s(l, skillsContainer)
      }),
        l(filtersArray, skillsFilterContainer)
      let e = document.querySelectorAll('#skills__filters__container .filter')
      i(e, r, filterTag, skillsContainer, 'skills')
    }
  })
  .catch((l) => {
    console.error('Error loading skills JSON', l)
  })
